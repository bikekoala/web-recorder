# Dependencies
node_modules/

# Build artifacts
dist/
*.tsbuildinfo

# Output (recordings, logs, traces, action logs)
output/
*.webm
*.mp4

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store

# Editors
.vscode/
.idea/

# Tool runtime artifacts — keep project-level Claude Code config in version
# control (hooks/skills/settings are team conventions), but exclude personal
# overrides + any local-state files the agent writes.
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/skills/
.claude/settings.local.json
